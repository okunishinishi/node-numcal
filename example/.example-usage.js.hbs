'use strict'

const numcal = require('numcal')

{{#each examples}}
//------------------------
// {{name}}
//------------------------
{{#each values}}
console.log(numcal.{{../name}}({{{args}}})) //-> {{result}}
{{/each}}

{{/each}}
